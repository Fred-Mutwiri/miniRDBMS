defmodule MiniRDBMS.Catalog do
  @moduledoc """
  Manages database metadata.

  Responsibilities:
  - table definitions
  - schemas
  - primary keys
  - unique constraints
  - index metadata

  The catalog is the source of truth for what exists in the database.
  """

  # Will become a GenServer in later commits
end
